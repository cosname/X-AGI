# 2026 Rising Stars Poster 报名论文

Poster 页的论文目录来自 2026 年 9 月 16 日收到的报名表，展示报名信息。
它不表示论文已获 X-AGI 最终录用，也不表示现场展示安排已确定。
这份名单与首页的 14 张分论坛宣传海报是两个独立数据集。
首页 lower page 在“演讲嘉宾”之后设置独立的“论文展示”板块，直接读取同一份 42 篇公开论文数据。
首页和 Poster 目录通过 `src/data/poster-research.ts` 统一按论文完整标题 A–Z 排列，忽略大小写并采用自然数字顺序，不改变生成文件中的报名数据顺序。
首页通过横向分栏展示论文首页预览与完整信息，并提供搜索、浏览控件和完整目录入口；Poster 内页保留逐篇目录及会议筛选。
首页论文区去掉外层玻璃卡片、会议徽章、重复数量徽章和内嵌圆形箭头，保留完整题名与紧凑的报名人、单位信息。
两块内容均位于历届影像之前，不以专题宣传海报替代研究论文展示。

## 本次整理

- 原表共 44 条 Rising Stars Poster 报名，审核状态均为“审核通过”。
- 排除 1 条标题和网址均为 `test2`、未附论文的测试记录。
- 尹新祥的两条 CodeHacker 报名合并为同一论文，使用 ACL Anthology 正式条目。
- CodeHacker 学校展示为“香港大学 / 西北工业大学”，保留首次报名填写的两所学校，省略括号中的个人阶段备注。
- 宋俊儒的两条记录对应不同论文、不同单位，分别保留，不凭同名合并身份。
- 最终展示 42 篇论文，共 41 个不同的报名姓名字符串。
- 15 条原始记录的题名经过补全或正式大小写、措辞校正，其中包含 CodeHacker 的重复记录。
- 所有论文均核对了完整题名及公开链接，逐条证据保存在 `src/data/poster-research-curation.json`。

大部分题名通过论文集、会议官网、arXiv、OpenReview、出版方索引或作者公开论文 PDF 核对。
PASTE-D2Net 的公开 ACM 页面本次无法读取，完整标题与 DOI 改由报名人提交的论文 PDF 首页核对，其区别及文件摘要已保存在校订记录中。
部分会议和期刊归属仅来自报名填写，页面明确说明此来源，不将其升级为独立确认的发表状态。
TMLR 条目中未核实的 ICLR 2027 Journal-to-Conference Track 说法未纳入页面。
期刊分区、影响因子、作者位次等评价不纳入目录。

2026 年 9 月 17 日提供的全量报名导出包含“论文PDF”和“论文录用证明（如OpenReview截图）”，没有独立 Poster 图片或 PDF 上传字段。
“海报标题”和“海报对应论文发表情况”是文字字段，“头像照片”也不是 Poster 素材。
按用户要求，全部 42 篇预览统一从参会者提交的“论文PDF”附件生成，明确标注“论文预览”和“PDF 首页”，不将其称为作者提交的 Poster 海报。
原表的 44 条 Poster 报名中，1 条测试记录没有论文附件，43 条实际报名均有附件；合并同一论文的重复报名后对应 42 篇论文。
点击预览、标题或“阅读论文”均打开核对过的公开论文来源。
原始附件链接、完整 PDF、录用证明和联系信息不进入网站或版本库；只输出已核对论文的首页图片。

## 论文预览更新

`src/data/paper-preview-sources.json` 由附件导入器生成，记录报名表摘要、来源类型、报名匹配键和每篇原始 PDF 的 SHA-256，不记录报名附件地址。
导入器可读取全量参会表，只选择已校订、审核通过的 Poster 报名；与目录导入器使用相同的报名键和重复报名首条保留规则。
任何当前论文缺少附件或出现未经校订的新论文，导入器会停止，避免混用其他来源或错配附件。
生成器核对 PDF 文件签名、摘要与首页题名，使用 Poppler 将完整首页渲染为 1400px PNG，再用 Sharp 输出 640px WebP。
PNG 保留在非公开 source archive，网页仅按需加载压缩后的 WebP；图片自带尺寸，采用 lazy loading 和 async decoding。
提交版本题名与目录正式题名存在措辞差异时，在校订记录的 `submittedPdfTitle` 中保存经人工核对的附件题名；目录仍保留正式题名。

```sh
npm run papers:import-submitted -- --workbook '/absolute/path/to/attendee-list.xls' output/submitted-paper-previews
npm run papers:render -- output/submitted-paper-previews
npm run assets:verify
```

附件只下载到忽略的 `output/` 缓存目录，命名为 `<paper-id>.pdf`，下载记录也只保存 URL 摘要和文件摘要。
`src/data/paper-preview-sources.json`、`src/data/paper-previews.generated.json` 及全局 archive manifest/checksums 均由脚本更新，不应手工修改。
生成器先核对整批 PDF 签名、摘要及题名，再替换图片，避免题名不符时产生部分更新。
构建验证要求全部论文都有来自报名附件的预览，并拒绝私有字段进入预览清单、题名或摘要过期、图片校验失败及缺失的预览链接。

## 数据边界与更新

公开模块仅含稳定 ID、论文标题、报名人、学校或单位、会议或期刊、公开论文链接六个字段。
邮箱、电话、订单、审核明细、简历、录用证明及报名平台附件链接不进入网站或版本库。
原始 XLS 保留在用户提供的位置，不作修改，也不复制进仓库。
输入文件 SHA-256 为 `38b4f605976e537450dfa56b9776c4a29144e41f9dea797a13448500fe2d88ea`。

执行导入前，应在校订 JSON 中为新记录填写完整标题、公开链接、会议或期刊和证据。
记录键由报名姓名、原填标题与原填链接计算，未知记录会停止导入，避免无依据补齐或静默替换。
同一论文只按校订后的稳定 ID 去重，同名不同论文保持独立。
导入器使用隔离的 LibreOffice profile 读取 XLS，转换产生的临时 CSV 在 `finally` 中删除。

```sh
npm run posters:sync-research -- --workbook '/absolute/path/to/attendee-list.xls'
npm test
```

`src/data/poster-research.generated.ts` 由导入器生成，不应手工编辑。
构建检查会验证公开字段、题名、链接、稳定 ID 和校订记录的一致性。
页面保留完整静态目录，JavaScript 只负责搜索、会议筛选和结果计数。
目录视觉沿用网站现有的 `--glass-shell-*`、`--glass-micro-*` 与 `--glass-lens-*` 材质变量，以及深蓝墨色、紫色和纸感底色。
搜索与筛选使用玻璃胶囊控件，论文列表保持清楚的文字层级，聚焦时呈现轻量玻璃高光。

## 本地验证

以下为本地验收记录，实际发布状态以提交合并、CI 和生产校验凭据为准。
直接检查包括导入的排除与去重计数、完整站点构建、静态页面公开信息、桌面和手机排版、组合筛选、清除筛选与无结果提示。
单元测试覆盖私有字段隔离、未审阅来源拒绝、去重规则与公开链接约束。
本次 127 项单元测试全部通过，Astro 检查为 0 errors、0 warnings，另有 9 条既有 `output/` 文件提示。
桌面 1440px、手机模拟 390px 与 320px 均无横向溢出，目录控件和论文链接点击区域高度均至少 44px。
浏览器核对了全部 42 条记录，原生搜索输入与 Enter 清除操作通过，组合筛选、同名论文、无结果和长会议名称筛选均正常。
手机验证使用浏览器视口模拟，未使用实体手机。
导入器重复运行结果为 `changed: false`，原始 XLS 的 SHA-256 与导入前一致。

## 2026 年 9 月 17 日附件预览验证

已从新表的“论文PDF”字段取得并核对全部 42 篇原始附件，表格 SHA-256 为 `849d639ac2fb4cf292f0c4dbf3a076445d056ef28287a23f14cc7ddeed161b53`。
9 篇此前缺失的预览已补齐，原有 33 篇也全部重新从报名附件生成，其中 18 篇提交 PDF 与原公开来源 PDF 的文件摘要不同。
Agentic Proposing 提交的是匿名稿，VKG-QA 的提交版题名与目录题名存在词序差异，两者均已核对并保留参会者提交的版本。
42 张首页均完成视觉检查，未发现误用录用证明或 Poster 图片的情况。
131 项单元测试通过，Astro 检查为 0 errors、0 warnings、0 hints，资源校验、整站构建和构建验证均通过。
本地浏览器核对目录页 42 张图片全部解码成功，首页同样包含 42 张预览且没有缺图提示。
首页及目录页的“王敏”搜索均正确显示 1 / 42，桌面 1440px 与手机模拟 320px 无横向溢出，目录另核对了 390px。
原始报名附件地址未进入公开预览清单、图片归档说明或构建 HTML。
浏览器的既有表单字段标识、其他模块懒加载图片尺寸提示不属于本次改动，未扩展处理。
本次为独立工作区内的本地修改和验证，不代表已合并或发布上线。
