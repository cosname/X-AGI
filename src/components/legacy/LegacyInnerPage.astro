---
import type { EditionConfig } from '../../config/site';

interface Props {
  edition: EditionConfig;
  title: string;
  status?: string;
  visual?: 'legacy' | 'goal';
}

const { edition, title, status, visual = 'legacy' } = Astro.props;
---

<header class:list={['page-header', visual === 'goal' && 'page-header--goal']}>
  {visual === 'goal' && <slot name="art" />}
  <div class="container custom-container header-content">
    <div>
      <h1>{edition.titleZh}</h1>
      <p class={status ? 'page-subtitle' : undefined}>
        {title}
        {status && <span class="status-badge status-open">{status}</span>}
      </p>
    </div>
  </div>
</header>

<slot />
