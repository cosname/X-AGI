---
import type { EditionConfig } from '../../../config/site';

interface Props {
  edition: EditionConfig;
  title: string;
  status?: string;
  meta?: string;
}

const { edition, title, status, meta } = Astro.props;
---

<header
  class="page-header page-header--goal"
  data-connection-stage
>
  <slot name="art" />
  <div class="published-inner-container header-content">
    <div>
      <h1>{edition.titleZh}</h1>
      <p class={status ? 'page-subtitle' : undefined}>
        {title}
        {status && <span class="status-badge status-open">{status}</span>}
      </p>
      {meta && <p class="page-header__meta">{meta}</p>}
    </div>
  </div>
</header>

<div id="main-content">
  <slot />
</div>
