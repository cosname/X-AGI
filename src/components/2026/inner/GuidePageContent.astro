---
import { conference2026 } from '../../../data/conference2026';
import hotelPlan from '../../../assets/2026/venue/beijing-friendship-hotel-plan.jpg';
import palaceFloor from '../../../assets/2026/venue/friendship-palace-floor-2.jpg';
import hotelOfferCode from '../../../assets/2026/venue/friendship-hotel-xagi-offer-code.png';
import { currentEdition, editionPath } from '../../../config/site';

const maps = [
  {
    ...conference2026.venue.maps[0],
    image: hotelPlan,
    width: 1551,
    height: 1551,
  },
  {
    ...conference2026.venue.maps[1],
    image: palaceFloor,
    width: 1846,
    height: 1304,
  },
];
---

<main class="main-content">
  <div class="published-inner-container">
    <div class="info-card">
      <div class="card-header">会议场地</div>
      <div class="card-body">
        <div class="venue-details">
          <p class="venue-address">
            <span class="guide-icon" aria-hidden="true">⌖</span>
            <span>{conference2026.venue.scheduleName}｜{conference2026.venue.nameEn}</span>
          </p>
          {maps.map((map) => (
            <figure>
              <img
                src={map.image.src}
                alt={`${map.title}。${map.description}`}
                class="venue-map"
                width={map.width}
                height={map.height}
                loading="lazy"
                decoding="async"
              />
              <figcaption>
                <strong>{map.title}</strong>
                {' '}
                {map.description}
              </figcaption>
            </figure>
          ))}
        </div>
      </div>
    </div>

    <div class="info-card">
      <div class="card-header">交通与住宿</div>
      <div class="card-body">
        <div class="service-section">
          <h4>交通</h4>
          <ul>
            <li>机场、火车站与公共交通路线待发布</li>
          </ul>
        </div>
        <div class="service-section">
          <h4>住宿</h4>
          <div class="hotel-offer">
            <ul class="hotel-offer__summary">
              <li>
                北京友谊宾馆为 X-AGI 大会提供专属优惠，微信扫码进入酒店小程序，使用专属编号 5328460 查看
                <time datetime="2026-10-16">2026.10.16</time>
                至
                <time datetime="2026-10-19">2026.10.19</time>
                期间的预订信息。
              </li>
            </ul>
            <figure class="hotel-offer__code">
              <div class="hotel-offer__code-image">
                <img
                  class="hotel-offer__code-graphic"
                  src={hotelOfferCode.src}
                  alt="友谊宾馆 X-AGI 大会专属优惠微信小程序码"
                  width="370"
                  height="370"
                  loading="lazy"
                  decoding="async"
                />
              </div>
              <figcaption>微信扫码查看专属优惠</figcaption>
            </figure>
          </div>
        </div>
      </div>
    </div>

    <section id="travel-grant" class="info-card" aria-labelledby="travel-grant-link-title">
      <h2 id="travel-grant-link-title" class="card-header">{conference2026.travelGrant.title}</h2>
      <div class="card-body">
        <p>参加 Rising Stars Poster 海报展示的本科生，可查看补助条件与申请流程。</p>
        <a href={`${editionPath(currentEdition, 'poster')}#travel-grant`}>查看本科生差旅补助计划</a>
      </div>
    </section>
  </div>
</main>
